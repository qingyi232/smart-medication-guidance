<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MedicationApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicationGuide"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MedicationGuide.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.RegisterActivity"
            android:theme="@style/Theme.MedicationGuide.NoActionBar" />
        <activity android:name=".activities.MainActivity"
            android:theme="@style/Theme.MedicationGuide.NoActionBar" />
        <activity android:name=".activities.AddMedicationActivity" />
        <activity android:name=".activities.MedicationDetailActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.RiskDetailActivity" />
        <activity android:name=".activities.StatisticsActivity" />
        <activity android:name=".activities.MedicationLogHistoryActivity" />
        <activity android:name=".activities.MedicationInfoActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />

        <service
            android:name=".services.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="medication_reminder" />
    </application>

</manifest>
